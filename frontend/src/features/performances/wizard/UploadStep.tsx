import type { WizardStepProps } from "../store/performanceWizard.store";

export default function UploadStep(_props: WizardStepProps) {
  return (
    <div>
      <h1 className="font-['Anton'] text-5xl uppercase">
        Upload Recording
      </h1>
    </div>
  );
}