import type { WizardStepProps } from "../store/performanceWizard.store";

export default function ReviewStep(_props: WizardStepProps) {
  return <h1 className="font-['Anton'] text-5xl uppercase">Review performance</h1>;
}